# SyncPay (legado)

Substituído por Mercado Pago (`mercadopago.*`, `MpPixDialog`, `MpCardForm`).

Arquivos mantidos apenas para referência / rollback:

- `syncpay.server.ts`
- `syncpay.functions.ts`
- `routes/api/public/syncpay-webhook.ts`

Não usar em novos fluxos de checkout.
